The data folder contains all the processed data utilised for model training and evaluation including the three versions of datasets and the additionally derived graph related features.

The model folder contains all the experimented models and model evaluation results.

The raw folder contains the labelled unprocessed data.